{
  "name": "packages",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "build:zip": "sh ./zip.sh",
    "changeset": "changeset",
    "ci:publish": "pnpm build && pnpm build:zip && changeset publish",
    "ci:version": "changeset version"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.5",
    "turbo": "^2.0.3",
    "typescript": "^4.8.4"
  },
  "packageManager": "pnpm@9.1.4"
}
